#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <stdlib.h>
#include <stdarg.h>
#include <limits.h>
#include <unistd.h>

char put_chary(char c);
int _printf(const char *format, ...);
int put_s(char *c);

#endif
